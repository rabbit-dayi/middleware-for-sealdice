name: Build middleware-a

on:
  push:
    paths:
      - 'middleware-a/**'
      - '.github/workflows/middleware-a-build.yml'
  pull_request:
    paths:
      - 'middleware-a/**'
      - '.github/workflows/middleware-a-build.yml'
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.25.3'
          cache: true

      - name: Configure Go env
        run: |
          go env -w GOPROXY=https://goproxy.cn,direct
          go env -w GOTOOLCHAIN=local

      - name: Download deps
        working-directory: middleware-a
        run: go mod download

      - name: Build Windows amd64
        working-directory: middleware-a
        run: GOOS=windows GOARCH=amd64 go build -v -o dist/middleware-a-windows-amd64.exe

      - name: Build Linux amd64
        working-directory: middleware-a
        run: GOOS=linux GOARCH=amd64 go build -v -o dist/middleware-a-linux-amd64

      - name: Build macOS amd64
        working-directory: middleware-a
        run: GOOS=darwin GOARCH=amd64 go build -v -o dist/middleware-a-darwin-amd64

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: middleware-a-dist
          path: middleware-a/dist/*